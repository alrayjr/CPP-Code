#include "Car.h"
//#include "Dealer.h"

/*bool isSportsCar(Car& car){
    if (car.getMaxSpeed() > 100)
        return true;
    else
        return false;
}*/


int main(){
	//Car car;
	Car car("Ford", "Mustang");
	//Car car3("Ford", "Mustang", "150");
	Car.printCarInfo();
	//Car2.printCarInfo();
	//Car3.printCarInfo();

	//cout << isSportsCar(Car3) << endl;

	return 0;
}
